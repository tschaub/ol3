import styled from '@emotion/styled';
import {baseSpacingPx} from '../util/typography.js';

export const Page = styled.div({
  display: 'flex',
  flexDirection: 'column',
  margin: 2 * baseSpacingPx
});
